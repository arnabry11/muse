<%= form_for( [@post, @post.comments.build] ) do |f| %>
<p>
  <%= f.label :"Reply to thread"%><br>
  <%= f.text_area :content%>
</p>

<p>
  <%= f.submit({disable_with: 'Submiting...', class: "button"}) %>
</p>
<% end %>
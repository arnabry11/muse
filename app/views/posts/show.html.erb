<%= link_to  "Home", root_path%><br><br>
<%= image_tag  @post.image.url(:medium) %>
<h1><%= @post.title %></h1>
<p><%=@post.link%></p>
<p><%=@post.description%></p>
<p>Author: <%=@post.user.name%></p>

<p><strong><%= link_to  "Like", like_post_path(@post), method: :get %></strong> [ <%= pluralize(@post.get_upvotes.size, "like") %> ] </p>
<p><strong><%= link_to  "DisLike", dislike_post_path(@post), method: :get %></strong> [ <%= pluralize(@post.get_downvotes.size, "dislike") %> ] </p>

<h2><%= pluralize(@post.comments.count, "Comment") %></h2>
<% @comments.each do |comment|%>
  <p>
    <%= comment.user.name%> :=>
    <%= comment.content%>
  </p>
<% end %>
<%= render "comments/form"%>

<%= link_to "Edit", edit_post_path(@post)%>
<%=  link_to  "Delete",  post_path(@post), method: :delete, data: {confirm: "Are you Sure?"}%>
